# Example configuration file for rstar-arc
# All parameters are optional and will override defaults

# Task parameters
task-name: "8be77c9e"

# Model parameters


# Search parameters
search-mode: "beam_search"  # Options: beam_search, mcts
max-depth: 7
beam-width: 10  # Number of "best candidates" to keep after each step
branching-factor: 10  # Number of candidates to generate per node

# System parameters


# Runtime parameters
verbose: true

